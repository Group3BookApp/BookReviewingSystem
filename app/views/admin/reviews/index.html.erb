<div class="row">
  <div class="col-md-12">
    <h1><%= t ".manage_review" %></h1>
    <p>
      <%= link_to t(".refresh"), admin_reviews_path, class: "btn btn-primary" %>
      <%= render "search" %>
    </p>
    <%= render "review" %>
    <%= paginate @reviews, theme: "twitter-bootstrap-3" %>
  </div>
</div>
