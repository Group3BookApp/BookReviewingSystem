<div class="row">
  <div class="col-md-10 col-md-offset-1">
    <h1><%= t ".manage_comment" %></h1>
    <p>
      <%= link_to t(".refresh"), admin_comments_path, class: "btn btn-primary" %>
      <%= render "search" %>
    </p>
    <%= render "comment" %>
    <%= paginate @comments, theme: "twitter-bootstrap-3" %>
  </div>
</div>
