<div class="row">
  <div class="col-md-10 col-md-offset-1">
    <h1><%= t ".manage_users" %></h1>
    <p>
      <%= link_to t(".refresh"), admin_users_path, class: "btn btn-primary" %>
      <%= render "search_name" %>
    </p>
    <%= render "not_found" %>
    <%= paginate @user, theme: "twitter-bootstrap-3" %>
    <%= render "user" %>
    <%= paginate @user, theme: "twitter-bootstrap-3" %>
  </div>
</div>
